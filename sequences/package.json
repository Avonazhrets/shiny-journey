{
  "name": "ssequence",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ssSequence+kondr__2.html",
    "precommit": "lint-staged"
  },
  "author": "",
  "license": "ISC",
  "lint-staged": {
    "*.{js,json,css}": [
      "prettier --write",
      "git add"
    ],
    "*.ts": [
      "prettier --write",
      "tslint --fix -c ./tslint.json 'src/**/*.ts'",
      "git add"
    ]
  },
  "devDependencies": {
    "@types/jquery": "^2.0.49",
    "husky": "^0.14.3",
    "lint-staged": "^7.2.0",
    "parcel-bundler": "^1.9.2",
    "prettier": "^1.13.5"
  },
  "dependencies": {
    "jquery": "^3.5.0"
  }
}
